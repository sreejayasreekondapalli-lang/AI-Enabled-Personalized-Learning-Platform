# MySQL connection settings
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_mysql_user
DB_PASSWORD=your_mysql_password
DB_NAME=your_database_name

# JWT secret used by backend for signing tokens
JWT_SECRET=replace_with_a_secure_secret

# Optional: file upload directory for learning materials
UPLOAD_DIR=uploads
